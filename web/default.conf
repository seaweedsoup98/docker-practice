server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    access_log /dev/stdout;
    access_log /logs/access.log;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /who {
        proxy_pass http://linux:8080;
    }
}
