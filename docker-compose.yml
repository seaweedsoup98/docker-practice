services:
  linux:
    build: ./linux
    image: basic-linux:1.0
    environment:
      APP_MODE: "${APP_MODE:-compose}"
      PORT: "8080"

  web:
    build: ./web
    image: basic-web:1.0
    depends_on:
      - linux
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - ./logs:/logs
